name: Continuous Integration

on:
  pull_request:
  push:
    branches:
      - trunk

jobs:
  tests:
    runs-on: ${{ matrix.operating-system }}
    continue-on-error: ${{ matrix.pre-release }}

    strategy:
      fail-fast: false
      matrix:
        operating-system: ['ubuntu-latest']
        php-version: ['7.1', '7.2', '7.3', '7.4', '8.0', '8.1', '8.2', '8.3', '8.4', '8.5', '8.6']
        pre-release: [false]
        include:
          - php-version: '8.5'
            phpunit-flags: '--display-deprecations --fail-on-deprecation'
          - php-version: '8.6'
            phpunit-flags: '--display-deprecations --fail-on-deprecation'
            pre-release: true


    name: CI on ${{ matrix.operating-system }} with PHP ${{ matrix.php-version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          tools: composer:v2
          coverage: none

      - name: Get composer cache directory
        id: composer-cache
        run: |
          echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ matrix.php-version }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-${{ matrix.php-version }}-

      - name: Install dependencies with Composer
        run: |
          composer update --no-interaction --prefer-dist --no-progress ${{ matrix.composer-flags }}

      - name: Run PHPUnit for Composer install
        run: |
          vendor/bin/phpunit ${{ matrix.phpunit-flags }} tests

      - name: Install PEAR
        run: |
          sudo apt-get install php-pear

      - name: Install package with PEAR
        run: |
          pear package-validate ./package.xml
          php ./pear-package-helper.php
          sudo pear install ./.pear-package/package.xml

      - name: Run PHPUnit for PEAR install
        run: |
          cd `pear config-get test_dir`
          ${GITHUB_WORKSPACE}/vendor/bin/phpunit ${{ matrix.phpunit-flags }} ./HTML_QuickForm2
